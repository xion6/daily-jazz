name: Daily Jazz Recommendation

on:
  schedule:
    - cron: '0 22 * * *'  # 毎日 07:00 JST（UTC 22:00 前日）
  workflow_dispatch:       # 手動実行もできるようにしておく

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  jazz:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install anthropic httpx

      - name: Fetch past songs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue list --label "daily-jazz" --limit 50 --json body \
            | jq -r '.[].body' \
            | grep -oP '(?<=\*\*曲名\*\*: ).+' \
            >> past_songs.txt || true
          echo "PAST_SONGS<<EOF" >> $GITHUB_ENV
          cat past_songs.txt >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Run daily jazz
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PAST_SONGS: ${{ env.PAST_SONGS }}
        run: python tasks/daily_jazz.py > jazz_output.txt

      - name: Create GitHub Issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TODAY=$(TZ=Asia/Tokyo date '+%Y-%m-%d')
          SONG=$(grep -oP '(?<=\*\*曲名\*\*: ).+' jazz_output.txt | head -1)
          ARTIST=$(grep -oP '(?<=\*\*アーティスト\*\*: ).+' jazz_output.txt | head -1)
          YEAR=$(grep -oP '(?<=\*\*年\*\*: ).+' jazz_output.txt | head -1)
          gh issue create \
            --title "🎷 $TODAY - $SONG / $ARTIST ($YEAR)" \
            --body "$(cat jazz_output.txt)" \
            --label "daily-jazz"
